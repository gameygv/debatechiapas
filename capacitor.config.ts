import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'top.dockerapps.debatechiapas',
  appName: 'DebateChiapas',
  webDir: 'dist'
};

export default config;
